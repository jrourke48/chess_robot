# Core chess logic
python-chess>=1.999
stockfish>=3.28.0

# Computer vision and image processing and robotic frame transformations
numpy>=1.24

# CV / ML pipeline
opencv-contrib-python>=4.8
tensorflow>=2.13
scikit-learn>=1.3
matplotlib>=3.8

# Optional but common for robotics serial interfaces (Hiwonder/LX-16A)
pyserial>=3.5
asyncio>=3.4
gpiozero>=2.0

# Web API
fastapi>=0.100
uvicorn[standard]>=0.20

# Note: Stockfish engine binary is still required separately.
# Install via winget/brew/apt and/or set STOCKFISH_PATH (see README).
